# Domain Configuration
DOMAIN=yourdomain.com
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=ChangeMe123

# Container runtime socket (used by socket-proxy). `deploy.sh` will auto-detect and
# inject this into `.env` if missing; override here if needed.
DOCKER_SOCKET=/var/run/docker.sock

# ------------------------------------------------------------
# Localhost / rootless-friendly ports
#
# Rootless Podman cannot bind privileged ports (<1024). If you
# run rootless, either set these to high ports (recommended for
# local dev), or run rootful (sudo) for standard ports.
# ------------------------------------------------------------
HTTP_PORT=8080
HTTPS_PORT=8443
SMTP_PORT=1025
SUBMISSION_PORT=1587
SMTPS_PORT=1465
IMAP_PORT=1143
IMAPS_PORT=1993
POP3_PORT=1110
POP3S_PORT=1995

# Database Configuration
MYSQL_ROOT_PASSWORD=SecureRootPassword123
MYSQL_PASSWORD=SecureDbPassword123
MYSQL_DATABASE=mail_dashboard

# Application Secrets
APP_SECRET=ThisTokenIsNotSoSecretChangeIt1234567890ABCDEF
REDIS_PASSWORD=RedisSecurePassword123
TWO_FACTOR_ISSUER=MailServer_Dashboard

# Security
DEFAULT_QUOTA_MB=1024
MAX_MESSAGE_SIZE=26214400

# Timezone
TZ=UTC

# Mail Configuration
POSTMASTER_ADDRESS=postmaster@${DOMAIN}
REPORT_RECIPIENT=${ADMIN_EMAIL}

# Server IP (for DNS records)
SERVER_IP=your.server.ip.address
SERVER_IPV6=your:server:ipv6:address

# Let's Encrypt
LETSENCRYPT_EMAIL=${ADMIN_EMAIL}
LETSENCRYPT_HOST=${DOMAIN}

# Optional webmail (Horde)
# Enable Horde container via `deploy.sh` by setting ENABLE_WEBMAIL=1
ENABLE_WEBMAIL=0
# Public URL the dashboard should link to for webmail (if enabled).
# Localhost example: http://localhost:8080/
HORDE_URL=

# Horde database credentials (separate from the dashboard database user)
HORDE_DB_NAME=horde
HORDE_DB_USER=horde
HORDE_DB_PASSWORD=ChangeThisHordeDbPassword123

# Mailserver TLS mode (docker-mailserver SSL_TYPE)
# - For production with real certs: letsencrypt
# - For localhost dev: none
MAILSERVER_SSL_TYPE=letsencrypt

# Horde Configuration
HORDE_ADMIN_USER=admin
HORDE_ADMIN_PASSWORD=HordeAdminPass123

# Paths
MAIL_SERVER_PATH=/var/mailserver
CERTIFICATE_PATH=/etc/letsencrypt

# API Keys (generate your own)
DKIM_SELECTOR=mail
SPF_RECORD=v=spf1 mx ~all
DMARC_RECORD=v=DMARC1; p=none; rua=mailto:dmarc@${DOMAIN}
DEFAULT_URI=http://localhost:8080